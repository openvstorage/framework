#!/usr/bin/env bash

if [[ ! -z "$2" && ! -f /etc/somerandomupgradefile ]]
then
    echo 'Please start upgrade through GUI because all nodes in the cluster need to be upgraded simultaneously'
    exit 1
fi
